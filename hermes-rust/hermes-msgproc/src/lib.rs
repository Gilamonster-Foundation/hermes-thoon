#![forbid(unsafe_code)]

//! Phase 4 (deferred) — message processing for the Hermes Agent.
//!
//! Placeholder. This crate is intentionally deferred: per `PLAN.md` we
//! revisit it only after Phases 1–3 ship and benchmarks confirm that
//! per-iteration message-loop overhead is the next real bottleneck.
